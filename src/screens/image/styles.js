import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    marginTop: 0,
  },
  image: {
    width: '100%',
    height: 200,
    borderRadius: 12,
  },
  contentContainer: {
    marginTop: 20,
  },
});
